#include<stdio.h>
int main()
{
    int no =0;
    if (no > 0 )
    {
        printf("positive");
    }
    else if (no < 0)
    {
    printf("negative");
    }
    else
    {
        printf("number is zero");
    }
}
